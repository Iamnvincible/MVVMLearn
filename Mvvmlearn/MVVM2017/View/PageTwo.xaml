<Page
    x:Class="MVVM2017.View.PageTwo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MVVM2017.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding PageTwoInstance,Source={StaticResource Locator}}"
    >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Page Two" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Grid.Row="1" Command="{Binding GoBackCommand}" Content="GOBACK" HorizontalAlignment="Center"/>
    </Grid>
</Page>
